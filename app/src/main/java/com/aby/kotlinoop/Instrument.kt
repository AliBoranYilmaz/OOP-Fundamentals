package com.aby.kotlinoop

interface Instrument
{
    // Kotlin -> It is OPTIONAL to write the body of an interface function
    // Java -> It is also OPTIONAL to write the body of an interface function

    fun info()
    {
        println("Instrument info")
    }
}